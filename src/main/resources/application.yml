logging:
  file:
    name: ./mcp.log
  pattern:
    console:
spring:
  main:
    banner-mode: off
    web-application-type: none
  ai:
    mcp:
      server:
        name: mcp_command-line_tool
        version: 1.0.0


ssh:
  host:
  port: 22
  username:
  passwd:
  privatekeypath:
